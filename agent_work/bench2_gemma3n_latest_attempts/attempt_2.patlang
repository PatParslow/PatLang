make a function called renderPrimeTester takes no arguments returns ui_component
 let prime_tester_ui = new("Container")

 let number_to_test_input = new("TextField", "Number to Test")
 let restriction_checkbox = new("Checkbox", "Restrict to Integers")
 let result_label = new("Label", "Result:")

 let submit_button = new("Button", "Calculate")

 let input_group = new("Group")
 let input1 = new("TextField", "Number to Test", number_to_test_input)
 let input2 = new("TextField", "", input_group)
 let checkbox_group = new("Group", restriction_checkbox)

 if restriction_checkbox.checked then
▁▁let input_group = input1 # input2 # checkbox_group
 else
▁▁let input_group = input1 # input2
 end

 let main_container = new("Container")
 let result_display = new("Label", "")
 let submit_area = new("Group", submit_button)

 let is_valid = false
 if input1.value != "" and input2.value != "" then
▁▁if restriction_checkbox.checked then
▁▁▁if input1.value.matches("^-?\\d+$") then # integer validation
▁▁▁▁is_valid = true
▁▁▁else
▁▁▁▁print("error: enter only integers")
▁▁▁end
▁▁else
▁▁▁# Allow arbitrary strings for prime calculation display purposes, no specific validation here.
▁▁▁is_valid = true
▁▁end
 else
▁▁is_valid = false
 end

 if is_valid then
▁▁submit_button.disabled = false
 else
▁▁submit_button.disabled = true
 end

 submit_area.add(submit_button)
 main_container.add(input_group)
 main_container.add(result_display)
 main_container.add(submit_area)

 prime_tester_ui = main_container

 return prime_tester_ui
 end

 make a function called calculate_prime_test takes number retuurns result string
▁▁if number <= 0 then
▁▁▁let result = "Invalid input"
▁▁else
▁▁▁# Basic primality test (can be replaced with more efficient algorithms)
▁▁▁let is_prime = true
▁▁▁let i = 2
▁▁▁while i * i <= number do
▁▁▁▁if number % i == 0 then
▁▁▁▁▁let is_prime = false
▁▁▁▁▁break
▁▁▁▁end
▁▁▁▁let i = i + 1
▁▁▁end
▁▁▁if is_prime then
▁▁▁▁let result = "Prime"
▁▁▁else
▁▁▁▁let result = "Not Prime"
▁▁▁end
▁▁end
▁▁return result
 end

 make a function called update_result_display takes result retuurns nothing
▁▁print(result)

 let prime_tester_ui = renderPrimeTester()

 # Example usage:
 let result1 = calculate_prime_test(7)
 print(result1)

 let result2 = calculate_prime_test(10)
 print(result2)

 let result3 = calculate_prime_test(-5)
 print(result3)


check_renderPrimeTester()
▁▁let ui = renderPrimeTester()
▁▁let number_to_test_input = ui.find("TextField", "Number to Test")
▁▁let restriction_checkbox = ui.find("Checkbox", "Restrict to Integers")
▁▁let result_label = ui.find("Label", "Result:")
▁▁let submiit_button = ui.find("Button", "Calculate")
▁▁let input1 = ui.find("TextField", "Number to Test")
▁▁let input2 = ui.find("TextField", "")
▁▁let is_valid = false

▁▁if input1.value != "" and input2.value != "" then
▁▁▁▁if restriction_checkbox.checked then
▁▁▁▁▁▁if input1.value.matches("^-?\\d+$") then # integer validation
▁▁▁▁▁▁▁▁is_valid = true
▁▁▁▁▁▁else
▁▁▁▁▁▁▁▁print("error: enter only integers")
▁▁▁▁▁▁end
▁▁▁▁else
▁▁▁▁▁▁is_valid = true
▁▁▁▁end
▁▁else
▁▁▁▁is_valid = false
▁▁end

▁▁if is_valid then
▁▁▁▁submiit_button.diabled = false
▁▁else
▁▁▁▁submiit_button.diabled = true
▁▁end

▁▁if submiit_button.diabled == false then # Check if the button is enabled, then proceed with testing to catch edge cases related to disabling it.
▁▁▁▁let result1 = calculate_prime_test(7)
▁▁▁▁print("PASS: test1")
▁▁▁▁let result2 = calculate_prime_test(10)
▁▁▁▁print("PASS: test2")
