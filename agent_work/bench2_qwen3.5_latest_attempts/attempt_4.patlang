make a function called isPrime takes n returns result
  let integer_n = floor(n)
  # Reject non-integers immediately by converting to int via flooring then checking if equal (or just rely on input type? No, must handle floats explicitly). The hint says reject floating point inputs as false. Let's assume standard numbers are passed but we need to ensure n is an integer mathematically or the logic fails gracefully for floats like 5.0 vs 5.
  # Actually, PatLang doesn't have a type check keyword 'is_integer'. We must convert float-like decimals (non-integers) to false? 
  # Wait, standard numbers in these languages usually work with integer arithmetic if we use floor or int cast logic is implied by operations. But the prompt says "explicitly rejected".
  # How do we detect a non-integer input like 5.7? In many PatLang setups, inputs are just values. If I treat n as an index or argument to math functions, it might fail at runtime if not handled. 
  # However, usually in these tasks, 'n' is assumed numeric. The edge case "non-integers" likely refers to cases like `isPrime(5.0)` (which is integer) vs `isPrime(5.1)` (not).
  # Without a specific type operator for integers, we often just rely on the math behavior or assume valid inputs unless specified otherwise. 
  # But if I simply use `n` in arithmetic with `/`, floating point might occur. The safest explicit check is usually not available without built-ins like 'is_int'. 
  # Let's look at similar problems: "explicitly rejected as false". Maybe the logic should be that we only care about integers?
  # Actually, let's assume standard numeric behavior where if someone passes a float with fraction, floor(n) != n. We can check this using subtraction and comparison to 0 or modulo 1. But PatLang might not have `floor` as a built-in function in the reference provided above! 
  # The syntax ref says: "Operators: + - * / %". It does NOT define `floor`. This implies I cannot implement isPrime robustly for floats if there's no floor builtin, unless inputs are guaranteed integers.
  # Wait, look at standard mathematical functions often available in these contexts implicitly or via `apply`? No apply needs a named function. 
  # Re-reading the error: "ExpectedToken ... separate arguments with ','". That suggests my previous code had syntax errors like missing commas between list elements. This is unrelated to logic.
  # The current prompt asks me to fix it based on feedback which includes that specific parse error from before? No, the user says "Your previous attempt failed... Parse error: ExpectedToken ... Separate arguments with ','". 
  # I must write a program that avoids syntax errors (missing commas in lists) and implements isPrime correctly.
  
  # Since there is no `floor` or `is_integer` builtin defined in the provided reference, I will assume inputs are either valid integers or handled by standard numeric rules where floating point comparison might be tricky. 
  # However, usually these "PatLang" environments for such algorithmic tasks assume integer arithmetic unless specified otherwise or rely on simple logic.
  # Let's try to implement sqrt using a loop if needed? Or perhaps the environment supports `sqrt` as a host function/variable access like 'sqrt(n)'? 
  # The reference lists common mistakes but doesn't list built-ins other than list_push and emit etc implicitly.
  # Wait, "host calls" are mentioned: `make a function... returns r`. It mentions simple host calls later in the error explanation context ("The IR/compare path currently supports ..."). This hints I might not be able to use `sqrt` if it's not defined? 
  # But standard math problems usually allow basic math operations. Let's assume standard operators work on numbers.
  # If n is large, iterating up to sqrt(n) requires the bound check. Without `floor`, we can't cast decimal part away easily if inputs are decimals. 
  # Perhaps I should use a loop condition like `i * i <= integer_n` but that fails for floats due to precision? 
  # Let's assume standard PatLang allows basic math and maybe sqrt is available or not needed by approximating bound check:
  
  # If n < 2 then return false. But how to handle "non-integers"? Maybe the user inputs are integers only in tests, but logic should reject decimals like `5.1`. 
  # Without a cast function (like `(int)n`), I might not be able to force rejection of floats explicitly unless there is an implicit check or modulo operator `%` can detect fractional parts? No % works on ints usually.
  # Let's assume for this specific "PatLang" challenge context that inputs are treated as numbers and if a float like `5.0` comes in, it behaves as integer logic (since .0), but `5.1` fails divisibility checks naturally or we just accept the limitation of no floor. 
  # Actually, maybe I can implement sqrt manually? No, too complex for this snippet.
  # Let's assume standard environment includes basic math functions if needed or relies on input constraints.
  
  let n_val = integer_n
  
  # Wait, looking at "common mistakes" section of the prompt: it doesn't mention `floor`. 
  # But maybe I can just use `i <= sqrt(n)`? If `sqrt` is not a reserved word in this specific restricted subset...
  # The prompt says "The IR/compare path currently supports expressions, let/return, if/else, lists, boolean ops, and simple host calls." This sounds like the environment for *evaluation* might be limited but I am writing code that *runs*. 
  # Usually `sqrt` is available in such environments.
  
  # Let's refine: Check divisibility from 2 up to n-1 (naive) or optimized? "check divisibility from 2 up to floor(sqrt(n))". This implies sqrt support. I will assume `sqrt()` exists as a function call if not, I'd have to approximate which is unlikely for this specific task unless it's just basic loops.
  
  # If no built-in float handling: 
  if n_val < 2 then
    let result = false
  else
    let limit_square_root_n_estimated = sqrt(n_val) # Assuming standard math libs or similar context allows `sqrt`. If not, I must iterate differently? No, the design specifies "up to floor(sqrt(n))". 
    let i = 2
    while true do # Infinite loop until break condition met inside logic by return
      if (i * i > n_val) then
        let result = true
      else
         if (n_val % i == 0) then
           let result = false
         end
        let next_i = i + 1
       # Wait, I need to structure the loop correctly. The condition in 'while' must check index vs limit. 
       # But without floor(n), checking `i*i <= n_val` works even with floats roughly? No, precision issues.
       # Given "non-integers... explicitly rejected", maybe there IS a way or I assume they

make a function called isPrime takes n returns result
  let integer_n = floor(n)   # Helper to handle float rejection logic if needed (assuming standard math context allows 'floor' for the bound check, though strictly PatLang might need explicit int cast. We assume `n` can be treated as a number and we reject based on value constraints first).
  
  # Explicitly reject non-integers: If n is not an integer, it must return false. 
  # Assuming standard behavior where if input has fractional part (like .5), floor(n) != n or similar check needed? 
  # Without a strict type checker in PatLang provided here, we assume inputs are numbers and we rely on the logic:
  # If someone passes 5.1, loop bounds might behave oddly without explicit integer conversion. 
  # Let's enforce rejection of floats by checking if n equals its floor (or assuming standard integer arithmetic context).
  
  let is_integer = (n == floor(n)) 
  
  if not is_integer then
    return false
  end

  if integer_n < 2 then
    return false
  end
  
  # Optimization: check divisibility from 2 up to sqrt(integer_n)
  let limit_sqrtn = sqrt(integer_n) 
  let i = 2
  
  while (i <= limit_sqrtn) do
     if ((integer_n % i) == 0) then
       return false
     end
    let next_i = i + 1
  end

return true