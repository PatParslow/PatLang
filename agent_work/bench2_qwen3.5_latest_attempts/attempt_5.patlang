make a function called isPrime takes n returns result
  if not (n % 1 == 0) then   # reject non-integers (floating point have remainder != 0 unless whole number, but we also need to handle floats properly by type check conceptually; PatLang doesn't have typeof so use modulo trick: if n has decimal part it's float. But simpler: just cast logic. Actually in math any integer n<=2 is false except... wait spec says n<=2 return false. So 0,1 ->false. What about negative? Usually primes defined on naturals >1. Spec says "values of zero and one also return false immediately". It implies other negatives might be considered not prime or we just handle small cases. Let's assume for simplicity: if n <= 1 then false. For non-integers, PatLang lacks typeof; but modulo with float inputs? The input is a number literal in tests likely integers or floats like 2.5. If n=2.5, does list_push work? No. But we can rely that the test framework passes numbers. We'll do: if (n % 1) != 0 then false # catches non-integers
    let result = false
    return result
  else if not ((-n < -3 or n > 2)) and n <= 2 ? No simpler logic needed? Actually better standard approach:

Re-thinking without fancy tricks: PatLang only has arithmetic. We can detect float by (char_code(to_string(n), to_int(length_of_fractional_part)? No). Alternative: assume that all test inputs are either integers or floats that we must reject if fractional part exists. But there's no direct way in expression-only return? Wait the spec says "explicitly rejected as false". In PatLang, maybe just treat any number with n % 1 != 0 as non-integer and hence not prime (return false). For negative numbers: usually not prime; but if we follow standard definition primes are >1. So handle all cases by first rejecting <=2 except... spec says "if n ≤ 2 return false" explicitly for any such input including negatives? It says values of zero and one also return false immediately, implying others in that range too (like negative) might be handled naturally or we need to define behavior. Typically prime function returns false on non-positive integers <=1? Actually definition: primes are positive integers >1. So n<2 ->false includes negatives.

Let's write clean code following spec literally without over-engineering type detection if tests only give valid numbers but sometimes floats; use modulo check for float rejection and then loop from 2 to sqrt(n). How to get integer square root? Use while with floor logic by computing next potential divisor up to current upper bound squared <= n.

Simplified robust implementation:
    let is_integer = (n % 1 == 0)
    if not is_integer or n < 2 then
      let result = false
      return result
    else
       let limit = floor(sqrt_approximation)? But no sqrt function in PatLang! Ah problem. We must compute up to integer i such that i*i <= n without a math library. So loop condition: while (i * i) < n do test; start checking at 2 and increment until (current_i +1)^2 > n? Actually simpler: just try dividing by each candidate d from 2 upwards; if we reach any divisor return false; but how to know when to stop without sqrt? We can loop while (candidate * candidate) < n do continue testing, else break. But note that for very large n this could be inefficient but acceptable and fits the spec: "loop terminates early upon finding any factor". For perfect squares we still check up to root exactly where condition changes from d*d <=n.

Correct control flow with while loop using candidate squared comparison as termination condition (or better, test until candidate > n/candidate? Use multiplication). The standard trial division without sqrt uses while(candidate * candidate) < n then next candidate; after that if no divisor found and the number is not 0 or 1 return true. But wait what about numbers like 2*3=6: start with d=2, 2*2<6 continue check divide by 2 -> remainder !=0? No, we need to check all divisors from 2 up to sqrt(n). When candidate > sqrt(n) the condition candidate*candidate < n becomes false and loop ends; if no divisor found then prime.

So structure:
    let d = 2
    while (d * d) <= n do   # include equality for exact square roots
        if (n % d == 0) then
          return false     # early exit, not needed but possible via returning directly? Spec says "returning true only if no divisor found". We can just use direct returns. But need to ensure all paths hit a return statement or we end at the end with result=true. To keep it simple and safe without relying on fall-through in PatLang (which might be allowed, but let's make explicit). Actually earlier examples show early returns are valid: "return r" even if never assigned; so returning false inside loop is fine.
          return false        # not needed to declare result variable then return later? We can just do direct return on finding divisor or else after loop set true and return once at end. But if we have nested ifs, need proper returns.

Refined version that matches PatLang's structure with single function definition:

make a function called isPrime takes n returns r
  # check integer requirement first (non-integers rejected)
    let is_int = (n % 1 == 0)   # but note: -2%1==0 in many languages? Assuming standard modulo behavior where remainder sign follows dividend, so negative integers also have zero mod. For positive floats like 2.5, n%1=0.5 !=0; reject them.
    if not is_int then
      return false
    end

    # case for small numbers <=2: spec says "if n ≤ 2" -> return false including negatives? But let's handle only positive ones since negative with mod==0 and integer will be filtered by the loop condition d*d<=n which fails immediately because n<4, but we also need to explicitly exclude negatives as they are not prime. So add explicit check for n < 3 (covers <=2 and any negatives)
    if n < 3 then   # since primes must be >1; numbers less than 2 or non-positive return false. For negative integers is_int==true but n<3 ->false, good.
      return false
    end

    let d = 2
    while (d * d) <= n do       # condition uses current square vs original number to avoid sqrt function; stops when next candidate's square exceeds n
        if (n % d == 0) then     # divisor found -> composite
          return false           # exit immediately, no need for extra result variable here due to early return syntax supported by examples in prompt? Wait example uses "return r" after computing once. But multiple returns are allowed as per PatLang semantics; the first valid path is taken. Just ensure all branches eventually reach a print or implicit success if not reached earlier? Actually we can use direct return for composites, then set r=true at end and return.
          return false           # redundant but harmless given control flow rules in prompt (they allow early returns as long as structure uses let/result only once before final return) -> better to have explicit result variable with assignments per branch style

make a function called isPrime takes n returns r
  # Reject non-integers: any number with fractional part (n % 1 != 0) is not prime.
    if (n % 1) != 0 then
      let r = false
      return r
    end

  # Primes must be > 1. 
  # Spec says n<=2 returns false immediately. This also covers negative numbers and 0,1 naturally since they are not >1.
    if (n <= 2) then
      let r = false
      return r
    end

  # Trial division: check divisibility from d=2 up to sqrt(n).
  # We simulate checking up to sqrt by looping while (d * d) < n. 
  # Note: For perfect squares k*k, when d=k, d*d=n. The loop condition usually is <= or we handle equality outside?
  # Standard logic: if (n % d == 0) return false for all d where d*d <= n. If no divisor found by the time d exceeds sqrt(n), it's prime.
  let d = 2
    while ((d * d) < n) do     # Optimization: strictly less than saves one iteration, but must check equality case if we were using loop differently? 
        # Wait: For n=4 (composite): d starts at 2. 2*2 is not < 4 (it's equal). Loop doesn't run? We would miss divisor 2!
        # Correction: Condition must be <= to catch square roots, or we rely on modulo check before loop termination logic changes. 
        # Better safe condition: while ((d * d) <= n) do ... but actually checking divisibility by d when d*d == n is crucial for squares like 4,9,25.
        let test = (n % d) != 0   # if remainder exists, continue
          break                  # This syntax "let x=cond; true" logic in PatLang? 
                                  # Let's rewrite loop body simply:
    end

Re-imagining the loop structure for clarity and correctness in PatLang without complex breaks inside `while` condition logic unless using standard flow:

Correct Pattern with explicit return on finding a divisor or falling through to success. We need to be careful about the perfect square case (e.g., n=9, d should reach 3).
Let's use <= for safety and handle returns carefully. Since PatLang allows multiple `let` but we want one function definition:

make a function called isPrime takes n returns result_var
    if ((n % 1) != 0) or (n < 2) then
      let result_var = false
      return result_var
    end
    
    # Loop from d=2 to sqrt(n). Using condition (d * d) <= n ensures we catch factors of perfect squares.
    let found_divisor = true   # assume composite until proven otherwise? No, loop will set it if divisor found. 
                              # Actually standard is: assume prime=true; for each factor return false.

Let's simplify flow using the "result variable" pattern from earlier examples in prompt where we accumulate or decide at end. But here early exit on composite is better.
However, to strictly follow PatLang structure without implicit fall-through ambiguity (though usually allowed), let's use a flag `is_prime` initialized true.

Final robust implementation:

make a function called isPrime takes n returns r
  # Step 1: Handle non-integers and small numbers as specified
    if ((n % 1) != 0) then   # Reject floats like 2.5, -3.7 etc.
      let r = false
      return r
    end
    
    if (n <= 2) then         # Cover 0, 1, negative numbers per spec "if n<=2"
      let r = false
      return r
    end

  # Step 2: Assume prime until a divisor is found in range [2, sqrt(n)]
  let d = 2
  while ((d * d) <= n) do     # Check up to square root inclusive
    if (n % d == 0) then      # Found a factor
      let r = false
      return r                # Return immediately for composite numbers
    end
    
    let d_plus_1 = d + 1
    let d = d_plus_1          # Increment loop counter variable 'd' directly? 
                              # PatLang doesn't have "for" loops in standard Python style, uses while with manual update.
                              # However, updating a declared `let` inside while body works:
      return true             # Logic error above! We cannot return success from every iteration step easily unless we restructure.

Restructuring to avoid returning 'true' prematurely and instead ensuring the loop finishes naturally if no divisor found? 
But early exit is desired for performance ("loop terminates early").
The trick in functional/declarative style with `while` without state mutation outside might be tricky, but PatLang likely allows mutating local variables.

Let's try standard imperative control flow assuming variable updates are allowed:

make a function called isPrime takes n returns result
    let r = true             # Default to prime
    
    if ((n % 1) != 0) then
      let r = false
      return r
    end
    
    if (n <= 2) then
      let r = false
      return r
    end

    let d = 2
    while ((d * d) <= n) do   # Check divisibility up to sqrt(n)
        if (n % d == 0) then
            let r = false     # Mark as not prime upon finding divisor
            break             # Exit loop early via keyword 'break' if supported? PatLang syntax might differ. 
                              # If `break` is not a keyword, we set state and rely on next logic or use recursive helper (not requested).
                              
Given the prompt examples didn't show complex breaks but simple sequential functions:

Alternative approach using recursion or just letting loop run fully with flag update? The spec says "loop terminates early". 
If PatLang supports `break`, great. If not, we rely on checking divisibility and if found set false then stop logic via another check?
Actually, looking at the first example in prompt: "print(query(...))" showed simple prints. Functions are defined with bodies.

Assuming standard control flow keywords like `break` or `return` work as expected (as hinted by early return examples):

Final Code Block Logic:
1. Check float rejection (`n % 1 != 0`). Return false if true.
2. Check small range (`n <= 2`). Return false if true.
3. Loop `d` from 2 while `d*d <= n`.
4. Inside loop, check remainder. If zero -> return false (early termination). 
   Wait: If we use `return`, it exits the function entirely skipping remaining iterations which is exactly "terminate early". Perfect.
5. After loop completes without returning true yet? We need to declare a result variable at start or end. 
   But if we find no divisors, we must return true eventually. The only way to do that after `return false` paths are exhausted inside the function is if there is an explicit final block OR the default value logic works (but Python/PatLang requires return).
   
Correct Pattern for early returns: Declare result at end? No, you can't skip execution past a returned line unless structure handles it. 
Since we must return `false` immediately on finding a factor, the only path remaining after the loop is "no factors found", so we return true there.

