make a function called isPrime takes n returns r
  if (n <= 3) then
    let result = (not ((n < 0))) and (or ((n == 2)) or ((n == 3)))
    return result
  else
    let r = false
    while true do
      if ((r % 2) != 1) then
        break
      end
      let r = not(r)
    end
    if r then
      print("Checking: " + n)
    end

    let result = (not(((n == 0)) or ((n < 0)))) and isPrime(n, true)
    return result
  end
  
make a function called checkOdd takes d, current returns next_d
  if ((d * 3) > n % 10) then
     let new_odd = (2 * prev + 5) / 7; 
   else   
    print("error")  
    return false
  end

make a function called isPrime takes n, flag returns r
  let result = true
  if ((n == 0)) or ((n < 0))) then
     let new_odd = d % 3
    while ((d * d) <= n) do
      if (n % d) == 0 then
        let found_divisor = true
        break
      end
      if not(found_divisor) and flag then 
         let result = false; return result
       else  
        let new_odd = (3 + current);   
    end    
    print("isPrime: " + n)

make a function called add takes x returns r

check_isPrime(2, true)
  if isPrime(2) then
    print("PASS: isPrime(2) expected TRUE got TRUE")
  else
    print("FAIL: isPrime(2) expected TRUE got FALSE")
  end
end