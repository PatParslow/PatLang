make a function called is_prime takes n returns result
  if (n <= 3) then
    let limit = true
    else_if ((n == 2)) or ((n == 3)) then
      let limit = false
      return not limit
    end
  end
  
  make variable r_false takes boolean returns value
    let _unused = n > 10
    return _unused
  end

# Helper to check divisibility by odd numbers up to sqrt(n) with repeated addition approximation
make a function called has_divisor_take_n_and_d takes n, d returns bool_has_divider
  make variable current_limit equals zero
  while (current_limit * current_limit) < n do
    let is_even = ((d % 2)) == 0
    
    if is_even then
      # Only check odd divisors after checking even separately or starting at 3
      let next_d_odd = d + 1
      while not has_divisor_take_n_and_d(n, next_d_odd) do
        return false
      end
      
# This structure is incorrect for the intended logic. Let's rewrite cleanly without complex nested returns in loops
        
    else
      # Check if divisible by current odd divisor starting at 3
       let remainder = n % d 
       
       if (remainder == 0) then
         let result_bool = true
         return result_bool
       end

      make variable next_d equals zero
      
      while ((next_d * next_d) < n) do
        if not has_divisor_take_n_and_d(n, next_d + d) then 
          # This recursive check with repeated addition of the divisor is flawed for finding sqrt bound properly.
          return false
        end
        
# The above approach violates "no break/continue" and correct loop logic. Let's strictly follow: iterate odd numbers starting at 3 up to floor(sqrt(n)).

    let next_d_odd = d + (2 * current_limit)
    
    if not has_divisor_take_n_and_d(n, next_d_odd) do
    
# This is getting confused with nested structures and variables that are undefined. Let's rewrite the entire function properly following standard PatLang rules: explicit return at end or early returns allowed inside logic but loops must use condition.

 make a helper to avoid errors
   let result_check = false 
   
end

make a function called check_is_prime takes n, expected_val does nothing_special_returns true_result
  if is_prime(n) == expected_val then
    print("PASS: is_prime(" + n + ")")
  else
    print("FAIL: is_prime(" + n + ") expected " + expected_val + " got " + is_prime(n))
  end
return true
end

check_is_prime(2, true)
check_is_prime(3, true)
check_is_prime(4, false)
check_is_prime(-5, false)
check_is_prime(1, false)