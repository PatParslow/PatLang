make a function called ledger takes action, amount returns result
  let balances = {"Alice": 0, "Bob": 0}
  let status = ""

  if action == "deposit" then
    let balances["Alice"] = balances["Alice"] + amount
  else if action == "withdraw" and balances["Alice"] >= amount then
    let balances["Alice"] = balances["Alice"] - amount
  else if action == "withdraw" and balances["Alice"] < amount then
    let status = "Insufficient funds"
  end

  return {"balances": balances, "status": status}
end

print(ledger("deposit", 100))
print(ledger("withdraw", 50))
print(ledger("withdraw", 200))


