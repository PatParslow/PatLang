# =============================================================================
# Auto-Synthesized PatLang Host Function Wrapper: file_size
# =============================================================================

make a function called file_size takes file_path returns result
  print("  [SYNTHESIZED EXEC] file_size...")
  let raw_out = exec_capture("powershell", "-Command", "(Get-Item '" + file_path + "').Length
")
  let a = 0
  while (a < raw_out.length) and ((char_code(raw_out, a) == 32) or (char_code(raw_out, a) == 9) or (char_code(raw_out, a) == 13) or (char_code(raw_out, a) == 10)) do
    let a = a + 1
  end
  let z = raw_out.length
  while (z > a) and ((char_code(raw_out, z - 1) == 32) or (char_code(raw_out, z - 1) == 9) or (char_code(raw_out, z - 1) == 13) or (char_code(raw_out, z - 1) == 10)) do
    let z = z - 1
  end
  let trimmed = substr(raw_out, a, z - a)
  print("[CONTRACT PASS] Synthesized function file_size returned: " + trimmed)
  return trimmed
end
