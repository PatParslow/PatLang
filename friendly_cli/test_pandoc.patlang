# Test pandoc execution via exec_capture in PatLang
include "../self_hosting/lib/includes.patlang"

let output = exec_capture("pandoc", "--version")
print("=== PANDOC TEST OUTPUT ===")
print(substr(output, 0, 80))
print("=== END PANDOC TEST ===")
