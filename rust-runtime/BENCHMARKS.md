# PatLang vs Rust: benchmark results

Generated by `cargo run --release --bin bench_harness`. Each variant run 10 times (first run discarded as warm-up); median and min wall-clock time reported, plus peak process memory (working set) sampled while each run executes.

All three variants for a category run the *same algorithm*. Rust and PatLang-compiled are both built with `-O` (no `Cargo.toml`/no dependencies for either — bare `rustc`, matching exactly how the PatLang toolchain itself compiles programs, see `compile_source_to_exe` in `rust-runtime/src/ir/hosts.rs`). PatLang-interpreted runs via `pat --ir-run` with no compilation step.

**Methodology note on memory sampling**: memory is sampled by polling the child process's working set from an external harness process (`sysinfo`), not via any in-process instrumentation. On this machine, a single targeted-PID refresh has a measured floor of roughly 20-80ms of latency, so every benchmark's workload size was chosen to run for at least a few hundred milliseconds even in its fastest (Rust) variant, giving the sampler multiple genuine opportunities to observe the process rather than missing it entirely between spawn and exit. Reported memory figures should be read as "peak observed via periodic external polling," not an exact high-water mark.

## fib(32) recursive

| variant | median | min | peak memory |
|---|---|---|---|
| Rust (native) | 29.2 ms | 26.2 ms | 0.00 MB |
| PatLang (compiled native) | 4.39 s | 3.99 s | 4.44 MB |
| PatLang (interpreted, `--ir-run`) | 4.32 s | 3.87 s | 5.10 MB |

## iterative sum loop (1..25,000,000)

| variant | median | min | peak memory |
|---|---|---|---|
| Rust (native) | 30.8 ms | 26.7 ms | 0.00 MB |
| PatLang (compiled native) | 9.17 s | 8.60 s | 4.34 MB |
| PatLang (interpreted, `--ir-run`) | 10.52 s | 10.06 s | 5.01 MB |

## list/vector build-and-sum (2,000,000 elements)

| variant | median | min | peak memory |
|---|---|---|---|
| Rust (native) | 24.7 ms | 22.3 ms | 0.00 MB |
| PatLang (compiled native) | 1.94 s | 1.81 s | 249.95 MB |
| PatLang (interpreted, `--ir-run`) | 1.90 s | 1.76 s | 250.47 MB |

## string concatenation (400,000 appends)

| variant | median | min | peak memory |
|---|---|---|---|
| Rust (native) | 26.2 ms | 22.7 ms | 0.00 MB |
| PatLang (compiled native) | 45.84 s | 38.01 s | 8.90 MB |
| PatLang (interpreted, `--ir-run`) | 42.61 s | 9.37 s | 9.41 MB |

## quicksort (50,000 elements)

| variant | median | min | peak memory |
|---|---|---|---|
| Rust (native) | 25.5 ms | 23.2 ms | 0.00 MB |
| PatLang (compiled native) | 1.13 s | 1.00 s | 20.50 MB |
| PatLang (interpreted, `--ir-run`) | 984.3 ms | 906.9 ms | 21.06 MB |

## object dispatch + events (50,000 objects/events)

| variant | median | min | peak memory |
|---|---|---|---|
| Rust (native) | 88.8 ms | 72.5 ms | 18.46 MB |
| PatLang (compiled native) | 418.9 ms | 385.3 ms | 59.00 MB |
| PatLang (interpreted, `--ir-run`) | 363.2 ms | 334.3 ms | 59.61 MB |

