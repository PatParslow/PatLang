# Reference for spec_library/block_model/call_with_return.feature: the SAME
# source as bare_call_tail_position.patlang's inner `src`, run directly as
# an ordinary PatLang program.

make a function called start returns r
  print(1)
  helper()
end

make a function called helper returns r
  print(2)
end

start()
