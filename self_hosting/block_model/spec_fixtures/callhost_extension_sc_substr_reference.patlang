# Reference for library_parity.feature (issue #159): the same two sc_substr
# calls run directly under the real engine (the third, deliberately
# nonexistent, call is omitted -- it exists only to prove block-model's own
# failure message for an unknown host name is unchanged).

make a function called start returns r
  let h = str_intern("hello world")
  print(sc_substr(h, 6, 5))
  let g = str_intern("héllo wörld")
  print(sc_substr(g, 1, 4))
end

start()
