# Driver fixture for spec_library/block_model/event_dispatch.feature (Phase
# 10), scenario 1: emit("greet", 7) should reach the `when greet` handler
# with event_name == "greet" and event_data == 7, and nothing else.

include "../../lib/lexer.patlang"
include "../../lib/parser.patlang"
include "../lower.patlang"
include "../interp.patlang"

let src = "when greet do
  print(event_name)
  print(event_data)
end

make a function called start returns r
  emit(\"greet\", 7)
  return finish()
end

make a function called finish returns r
  print(99)
end
"

let toks = tokenize(src)
let ast = parse_program(toks)
let prog = bm_lower_program(ast)
bi_run(prog)
