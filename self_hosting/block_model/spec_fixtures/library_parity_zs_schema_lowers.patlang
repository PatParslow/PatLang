# Driver fixture for spec_library/block_model/library_parity.feature:
# Phase 18's own original checkpoint, finally met (as far as LOWERING
# goes -- see that file's own header for what's still not attempted:
# actually RUNNING the full zs_schema selftest suite, which needs
# test.patlang and several more files to ALSO lower, a separate, larger
# undertaking). self_hosting/lib/zs_schema.patlang -- real, unmodified
# library code (after Phase 21/22's own get_global/set_global/handler_*
# port, itself verified against zero regressions in every real-engine
# test suite that exercises it) -- now lowers completely through
# bm_lower_program, with no unsupported construct left anywhere in it.

include "../../lib/lexer.patlang"
include "../../lib/parser.patlang"
include "../lower.patlang"

let src = read_file("self_hosting/lib/zs_schema.patlang")
let toks = tokenize(src)
let ast = parse_program(toks)
let prog = bm_lower_program(ast)
print("zs_schema.patlang lowered completely")
