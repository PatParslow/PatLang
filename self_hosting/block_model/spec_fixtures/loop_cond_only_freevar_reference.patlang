# Reference for spec_library/block_model/loop_blocks.feature (Phase 22):
# the SAME source as loop_cond_only_freevar.patlang's own inner `src`,
# run directly as an ordinary PatLang program (no block model at all).

make a function called start returns r
  let n = 3
  let i = 0
  while i < n do
    print(i)
    let i = i + 1
  end
  print(999)
end

start()
