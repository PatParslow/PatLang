# Driver fixture for spec_library/block_model/pattern_matching.feature
# (Phase 11), scenario 5: only literal arms, no wildcard, run against a
# scrutinee none of them match -- a guaranteed-fail error, not a silent
# no-op (mirrors the real lower_match's own issue #44 follow-up decision).

include "../../lib/lexer.patlang"
include "../../lib/parser.patlang"
include "../lower.patlang"
include "../interp.patlang"

let src = "make a function called start returns r
  let x = 9
  match x do
  case 1 then
    print(100)
  case 2 then
    print(200)
  end
end
"

let toks = tokenize(src)
let ast = parse_program(toks)
let prog = bm_lower_program(ast)
bi_run(prog)
