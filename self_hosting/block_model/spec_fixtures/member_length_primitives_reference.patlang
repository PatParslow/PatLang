# Reference for spec_library/block_model/library_parity.feature (Phase 22,
# second finding): the SAME source as member_length_primitives.patlang's
# own inner `src`, run directly as an ordinary PatLang program (no block
# model at all) -- what pat --ir-run's own real interpreter produces for
# it, cross-checked against the block-model engine's own output for the
# identical source.

make a function called start returns r
  let s = "hello"
  print(s.length)
  let xs = [10, 20, 30]
  print(xs.length)
end

start()
