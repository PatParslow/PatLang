# Fixture for spec_library/block_model/native_codegen.feature (Phase 19).
# A failing require aborts before the rest of the body runs, through
# REAL native codegen -- ContractFail translates to the same
# contract_check(func_name, kind, text, ok) CallHost convention
# lower.patlang itself already emits for Assert.

make a function called start returns r
  require 1 > 2
  print(999)
end
