# Fixture for spec_library/block_model/native_codegen.feature (Phase 9).
# The same loop-free, two-block shape as spec_fixtures/two_block_jump
# .patlang, built here through the REAL native codegen path (self_hosting/
# block_model/tools/build_and_run_native.sh) instead of bi_run.

make a function called start returns r
  let x = 10
  let y = 20
  return combine(x, y)
end

make a function called combine takes a, b returns r
  let s = a + b
  print(s)
end
