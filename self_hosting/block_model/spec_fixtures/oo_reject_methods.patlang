# Driver fixture for spec_library/block_model/object_orientation.feature
# (Phase 13), scenario 4b: a class declaring a method is rejected plainly
# at lowering time.

include "../../lib/lexer.patlang"
include "../../lib/parser.patlang"
include "../lower.patlang"
include "../interp.patlang"

let src = "class Foo do
  field x = 1
  make a function called bar returns r
    print(2)
  end
end

make a function called start returns r
  print(1)
end
"

let toks = tokenize(src)
let ast = parse_program(toks)
let prog = bm_lower_program(ast)
bi_run(prog)
