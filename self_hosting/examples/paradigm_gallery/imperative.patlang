# Imperative: a sequence of statements that directly mutate state,
# step by step, in the order written. No functions, no objects, no
# closures -- just "do this, then do that."
let total = 0
let i = 1
while i <= 5 do
  let total = total + i
  let i = i + 1
end
print(total)
