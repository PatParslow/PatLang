# Stress test for exec_capture's READ LOOP specifically (not just a
# single small read): dir on a large directory produces enough output
# to require multiple ReadFile calls through the pipe.
let out = exec_capture("cmd", "/c", "dir", "C:\Windows\System32")
let len = rt_str_len(out)
rt_print_i64(rt_fixnum_unpack(len))
