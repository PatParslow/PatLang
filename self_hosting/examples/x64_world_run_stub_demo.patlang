# Native x64 has no clean-interpreter run: host_caps says so, and world_run
# answers with a checkable ok=false instead of crashing.
let caps = host_caps()
print(list_len(caps))
print(caps[0])
let r = world_run([], [])
print(r[2])
print(r[1])
