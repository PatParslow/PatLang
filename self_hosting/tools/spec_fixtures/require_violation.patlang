make a function called safe_divide takes a, b returns r
  require b != 0
  let r = a / b
  ensure (r * b) == a
  return r
end

print("safe_divide(0, 0) = " + safe_divide(0, 0))
print("(unreachable)")
