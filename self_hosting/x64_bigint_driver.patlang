# Compiles lib/x64_runtime.patlang + examples/x64_bigint_demo.patlang,
# proving the numeric-tower's first working cut (BigInt, dynamic-mode
# `+` dispatch). Run from the repo root:
#   rust-runtime/target/release/pat --ir-run self_hosting/x64_bigint_driver.patlang

include "lib/lexer.patlang"
include "lib/parser.patlang"
include "lib/lower.patlang"
include "lib/codegen_x64.patlang"

let src = read_file("self_hosting/lib/x64_runtime.patlang")
let src = src + chr(10) + read_file("self_hosting/examples/x64_bigint_demo.patlang")

let toks = tokenize(src)
let ast = parse_program(toks)
let ir = lower_program(ast)
let asm = emit_program_x64(ir, true, [])

write_file("self_hosting/build/x64_bigint_demo.asm", asm)
print("wrote self_hosting/build/x64_bigint_demo.asm")
