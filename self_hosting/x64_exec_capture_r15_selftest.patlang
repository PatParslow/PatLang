# exec_capture selftest (GitHub #25 roadmap): specifically exercises
# x64_os_exec_run_asm, the primitive that had the r15/rt_heap_alloc
# stack-corruption bug found via WinDbg (patc1_final.exe segfaulting
# the first time any --x64-compiled program ever called exec_capture
# at runtime -- see codegen_x64.patlang's own comment on the fix).
print(exec_capture("cmd.exe", "/c", "echo", "hello from exec_capture"))
