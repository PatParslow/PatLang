# abs()'s int-classification fast path (see x64_abs_asm's header):
# plain fixnum input should take the integer-negate branch, never
# touching the C library fabs() call at all.
let n = 0 - 7
print(abs(n))
print(abs(7))
