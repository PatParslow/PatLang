{
  "instruction": "fact_query",
  "category": "logic",
  "feature_file": "fact_query.feature",
  "source": "hand-authored",
  "verification_status": "verified",
  "verification_note": "fact(pred, a, b) records one binary fact; query(pred, a, _) returns a COUNT of facts matching (pred, a, *) -- the third argument is accepted but IGNORED (matches ANY b, not a specific one) -- confirmed: alice has 2 'likes' facts (bob, carol), query counts both regardless of what's passed as the third argument"
}
