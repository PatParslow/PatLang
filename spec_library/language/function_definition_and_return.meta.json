{
  "instruction": "function_definition_and_return",
  "category": "core",
  "feature_file": "function_definition_and_return.feature",
  "source": "hand-authored",
  "verification_status": "verified",
  "verification_note": "make a function called NAME takes P1,...  returns R ... end binds parameters by name for the function's body; `return EXPR` exits immediately with that value -- an if-guarded early return prevents later statements from running; a function with NO return statement anywhere does NOT error and does NOT implicitly return its last expression -- it returns Value::Unit, confirmed to print as a single BLANK LINE via a known-Unit-returning call (set_var), not the string "nil"/"undefined" and not a silently-dropped print with no output at all"
}
