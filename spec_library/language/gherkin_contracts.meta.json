{
  "instruction": "gherkin_contracts",
  "category": "goap_synthesis",
  "feature_file": "gherkin_contracts.feature",
  "source": "hand-authored",
  "verification_status": "verified",
  "verification_note": "Verified end-to-end via self_hosting/examples/goap_contract_bdd_demo.patlang's own 4 scenarios (run through self_hosting/lib/test.patlang's real Gherkin runner, not just described): a require/ensure clause checked immediately against a hand-written function's bound value (Slice 1); the same clauses checked against a genuinely GOAP-synthesized action's own binding, parsed out of plan()'s action_instance_label string rather than encoded as a GroundFact (Slice 2); a deliberately-violating synthesized candidate (scale(X=15) against require X<10) correctly detected and rejected, not silently accepted (Slice 2's third scenario); and a direct source-text inspection confirming self_hosting/lib/gherkin_contracts.patlang never calls contract_check(...) itself (Slice 3), with that check proven to actually discriminate by temporarily inserting a fake contract_check( marker and confirming it failed before removing it. All 4 scenarios verified identical across --ir-run, --patc, and patc1.exe (GitHub issue #12)."
}
