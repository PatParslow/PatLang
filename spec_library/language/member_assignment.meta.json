{
  "instruction": "member_assignment",
  "category": "object_model",
  "feature_file": "member_assignment.feature",
  "source": "hand-authored",
  "verification_status": "verified",
  "verification_note": "`obj.prop = value` (dot-notation assignment) lowers to the same send(obj, "set", prop, value) call as explicit send(...); a value written via dot-assignment is readable back via EITHER dot-notation (o.color) or get(o, "color") -- both read paths agree, confirming they share the same underlying field store"
}
