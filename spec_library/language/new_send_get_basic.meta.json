{
  "instruction": "new_send_get_basic",
  "category": "object_model",
  "feature_file": "new_send_get_basic.feature",
  "source": "hand-authored",
  "verification_status": "verified",
  "verification_note": "new(class, id) creates an object with TWO implicit auto-populated fields -- "type" (the class name) and "name" (the object's own id) -- confirmed present even with NO class ever declared; send(obj, "set", field, val) writes a field; get(obj, field) reads it back"
}
