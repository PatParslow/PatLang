{
  "instruction": "rule_declaration_sugar",
  "category": "logic",
  "feature_file": "rule_declaration_sugar.feature",
  "source": "hand-authored",
  "verification_status": "verified",
  "verification_note": "`rule Head(...) :- Body1(...), Body2(...).` is pure syntax sugar producing EXACTLY the same observable result as a hand-written rule_add(head_pred, head_args_list, body_list) call -- confirmed via the identical grandparent/parent chained-resolution scenario already verified for rule_add/solve directly, now via the sugar form"
}
