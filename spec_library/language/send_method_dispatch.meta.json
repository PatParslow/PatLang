{
  "instruction": "send_method_dispatch",
  "category": "object_model",
  "feature_file": "send_method_dispatch.feature",
  "source": "hand-authored",
  "verification_status": "verified",
  "verification_note": "send(obj, "method", args...) invokes a REAL user-defined method (a genuine closure call, resolved by walking the object's class then its parent chain) when one exists -- Person's own speak() overrides Animal's; an Animal instance (no override) calls Animal's speak() -- confirmed via actual returned message text, not just 'it didn't error'"
}
