{
  "instruction": "symbolic_materialize_bounded_range",
  "category": "symbolic_math",
  "feature_file": "symbolic_materialize_bounded_range.feature",
  "source": "hand-authored",
  "verification_status": "verified",
  "verification_note": "materializing the quadratic's Solutions against [0,10] correctly keeps only the positive root; materializing the sin Family against [-1,7] correctly returns exactly 3 members (0, pi, 2*pi) by walking n outward and stopping once a direction leaves the range; calling sym_solve_for with no closed form and no range returns an Error rather than guessing; the x+sin(x)=0 numeric fallback over [-10,10] finds exactly 1 root, a one-ULP bracket around the true root at x=0. Real bug found and fixed during verification: sym_solve_numeric_bounded's range endpoints arrived as exact Ints (from sym_interval(-10,10)), so (a+b)/2 stayed in unlimited-precision exact-Rational arithmetic instead of collapsing to float precision the way the hand-verified sqrt(2) bisection does -- the loop ran to a denominator near 2^1074 instead of terminating after roughly 60 steps; fixed by forcing Float coercion (* 1.0) on the range endpoints before bisecting"
}
