{
  "instruction": "task_registry_beacon",
  "category": "stdlib_task_registry",
  "feature_file": "task_registry_beacon.feature",
  "source": "hand-authored",
  "verification_status": "verified",
  "verification_note": "tr_announce(task, description) writes a single-row presence beacon; tr_read_one reads it back; tr_discover_all(max_age_ms) finds it when the age window is generous; a SECOND tr_announce for the same task OVERWRITES the row (confirmed via an ensure guard: exactly 1 row remains, not 2) -- a genuinely different persistence shape from queue_publish's own accumulating log, even though both are built on the same q_read/q_write primitives; real disk-file teardown explicitly removed and re-confirmed gone"
}
