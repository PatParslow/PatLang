{
  "instruction": "tcp_close_teardown",
  "category": "stdlib_networking",
  "feature_file": "tcp_close_teardown.feature",
  "source": "hand-authored",
  "verification_status": "verified",
  "verification_note": "after tcp_close(client), a tcp_read on the PEER connection returns the empty string (a graceful close signal), never an error and never hangs -- confirmed via a passing ensure guard on the ACTUAL post-close read, not merely that tcp_close itself didn't error"
}
