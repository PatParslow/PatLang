{
  "name": "sum_1_to_n",
  "kind": "tier_a_pure_logic_induction",
  "mechanism": "synth3_induce_accumulator (friendly_cli/lib/synth3_accumulator_search.patlang) -- a GENERIC extension to synthesis_recursive.patlang's own search space, searching a richer metarule shape: target(X,Y):-zero(X),zero(Y). target(X,Y):-chain(X,X2),target(X2,Y2),combine(Y2,X,Y). Not written for this problem specifically -- a real, reusable search procedure over (chain_pred, combine_pred) candidate pairs.",
  "llm_involved": false,
  "hand_authored_by_a_person": false,
  "search_candidates_tried": {
    "chain_preds": ["pred1", "pred2"],
    "combine_preds": ["plus", "times"],
    "note": "pred2 (step-by-2) and times (multiplication) were DELIBERATE DECOYS -- both real, independently-verified background relations, but genuinely wrong for this target. The search correctly rejected both and found (pred1, plus) as the first candidate pair that fit the visible examples."
  },
  "found_clause": "rule target(X, Y) :- zero(X), zero(Y).\nrule target(X, Y) :- pred1(X, X2), target(X2, Y2), plus(Y2, X, Y).\n",
  "visible_queries_used_for_search": [
    ["target", "1", "1", true], ["target", "3", "6", true], ["target", "2", "5", false]
  ],
  "blind_held_back_queries": [
    ["target", "5", "15", true], ["target", "10", "55", true],
    ["target", "10", "56", false], ["target", "7", "27", false]
  ],
  "blind_check_result": "ALL 4 PASSED -- including two negative near-misses (10,56 and 7,27, both plausible-looking wrong totals), confirming genuine generalization",
  "verification_status": "verified",
  "provenance": "direct response to: 'now, REMEMBERING that we are trying to build a system that can infer the steps needed to solve a problem WITHOUT LLM interference... let's try working out how to get these to work (let's drop the protocol of trying to use a local LLM for now, and you can avoid writing any code for these curriculum solutions unless it is to verify the machine produced one is correct or not)'. The earlier reference_solutions/sum_1_to_n.meta.json (a hand-specified Peano rule) is SUPERSEDED by this entry -- this time the identical structure was found by a real search procedure, not written by a person. Only the SEARCH INFRASTRUCTURE (synth3_accumulator_search.patlang) was authored by a person -- the target predicate's own clause body was not.",
  "supersedes": "reference_solutions/sum_1_to_n.meta.json (an earlier hand-specified version, since removed -- this search-found entry replaces it entirely, no need to keep both)"
}
