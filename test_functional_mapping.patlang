make a function called double takes: x {
  return x * 2
}

make a function called map takes: f, list {
  if list == [] then
    return []
  else
    return [call f with list[0]] + call map with f, list[1:]
  end
}

print(call map with double, [1, 2, 3, 4])