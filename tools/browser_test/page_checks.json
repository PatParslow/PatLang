{
 "sqlconsole|patlang-sql-console": "(async()=>{ const sleep=(ms)=>new Promise(r=>setTimeout(r,ms)); const flat=(s)=>String(s).split(String.fromCharCode(10)).join(' | ');\n  const inp=document.getElementById('sql-input');\n  const run=async(s)=>{ inp.value=s; await sqlSubmit(); await sleep(300); };\n  await run(\"CREATE TABLE people (id INT, name STRING, age INT)\");\n  await run(\"INSERT INTO people VALUES (1, 'alice', 30)\");\n  await run(\"SELECT * FROM people WHERE age > 20\");\n  const txt=document.getElementById('sql-result').innerText+' '+document.getElementById('sql-table').innerText;\n  return JSON.stringify({ok:txt.indexOf('alice')>=0, detail:flat(txt).slice(0,160)}); })()",
 "repl|patlang-repl": "(async()=>{ const sleep=(ms)=>new Promise(r=>setTimeout(r,ms)); const flat=(s)=>String(s).split(String.fromCharCode(10)).join(' | ');\n  document.getElementById('repl-input').value='print(6 * 7)';\n  await replSubmit(); await sleep(2000);\n  const t=document.getElementById('repl-transcript').innerText;\n  return JSON.stringify({ok:t.indexOf('42')>=0, detail:flat(t).slice(-160)}); })()",
 "maze|patlang-maze": "(async()=>{ const sleep=(ms)=>new Promise(r=>setTimeout(r,ms)); const flat=(s)=>String(s).split(String.fromCharCode(10)).join(' | '); await mzGenerate(); await sleep(1500); const c=document.getElementById('mz-canvas'); const cells=c.children.length; await mzSolveInstant(); await sleep(2000); const path=c.querySelectorAll('.mz-path').length; const st=document.getElementById('mz-status').innerText; return JSON.stringify({ok:cells>50 && path>0, detail:'cells='+cells+' path cells='+path+' status='+flat(st).slice(0,80)}); })()",
 "report-writer|patlang-report-writer": "(async()=>{ const sleep=(ms)=>new Promise(r=>setTimeout(r,ms)); await rpGenerate(); await sleep(2500); const ch=document.getElementById('rp-chart'); const st=document.getElementById('rp-status').innerText; return JSON.stringify({ok:ch.innerHTML.length>200, detail:'chart html='+ch.innerHTML.length+' status='+st.slice(0,100)}); })()",
 "cga|patlang-cga-classifier": "(async()=>{ const sleep=(ms)=>new Promise(r=>setTimeout(r,ms)); await sleep(1500); const before=document.getElementById('cga-status').innerText; cgaNewLayout(); await sleep(4000); const st=document.getElementById('cga-status').innerText; const grid=document.getElementById('cga-grid'); return JSON.stringify({ok:(grid.innerHTML.length>50 || grid.children.length>0 || st.length>0), detail:'status before='+before.slice(0,60)+' after='+st.slice(0,80)+' grid='+grid.innerHTML.length}); })()",
 "ide|patlang-ide": "(async()=>{ const sleep=(ms)=>new Promise(r=>setTimeout(r,ms)); const flat=(s)=>String(s).split(String.fromCharCode(10)).join(' | ');\n  const ed=document.getElementById('ed'); ed.value='print(6 * 7)';\n  const btn=Array.from(document.querySelectorAll('button')).find(b=>b.textContent.trim()==='Run');\n  btn.click(); await sleep(3000);\n  const out=document.getElementById('out-ide').innerText;\n  return JSON.stringify({ok:out.indexOf('42')>=0, detail:flat(out).slice(0,120)}); })()",
 "schemas-and-scenarios": "(async () => { const a = JSON.parse(await ((async () => {\n  const sleep = (ms) => new Promise((r) => setTimeout(r, ms));\n  const sel = document.getElementById('exsel'), out = document.getElementById('out-playground');\n  const btn = Array.from(document.querySelectorAll('button')).find((b) => b.textContent.indexOf('Compile and run') >= 0);\n  const rows = []; let ok = true;\n  for (const o of Array.from(sel.options)) {\n    sel.value = o.value; sel.dispatchEvent(new Event('change'));\n    out.textContent = '(not run yet)';\n    const t0 = Date.now(); btn.click();\n    let txt = '';\n    while (Date.now() - t0 < 25000) {\n      await sleep(300); txt = out.innerText;\n      if (txt.indexOf(' passed, ') >= 0 || txt.indexOf('[error') >= 0) break;\n    }\n    const pass = txt.indexOf('ALL TESTS PASSED') >= 0 && txt.indexOf('[error') < 0;\n    if (!pass) ok = false;\n    const line = txt.split(String.fromCharCode(10)).filter((l) => l.indexOf(' passed, ') >= 0)[0] || txt.slice(-80);\n    rows.push(o.value + ': ' + line.trim() + ' (' + ((Date.now() - t0) / 1000).toFixed(1) + 's)');\n  }\n  return JSON.stringify({ ok, detail: rows.join('; ') });\n})())); const b = JSON.parse(await ((async () => {\n  const sel = document.getElementById('zsx-sel');\n  const want = {\n    loans_bug: ['ReturnBook reaches a state that breaks an invariant'],\n    loans_ok: ['Explored every reachable state: 44 states, 186 transitions'],\n    vending_ok: ['Consistent: every fact is enforced by BuyVM'],\n    vending_free: ['Requirement missing from the schema'],\n    loans_larger: ['Explored every reachable state: 668 states, 5052 transitions'],\n    outcome: ['the schema computes a different outcome from the claim', 'Inconsistent'],\n    negative: ['rejection supported', 'the named reason is among the failing clauses', 'Consistent'],\n    sequence: ['realised by running the schema', 'Consistent'],\n    generate: ['Scenario: BuyVM succeeds', 'Scenario: BuyVM is rejected', 'no independent witness for: item in snacks', 'Consistent'],\n    refine: ['Refinement holds over']\n  };\n  const rows = []; let ok = true;\n  for (const o of Array.from(sel.options)) {\n    sel.value = o.value; zsxLoad();\n    await zsxRun();\n    const txt = document.getElementById('out-zsx').textContent;\n    const needed = want[o.value] || [];\n    const missing = needed.filter((s) => txt.indexOf(s) < 0);\n    const hasError = txt.indexOf('[error') >= 0;\n    const pass = missing.length === 0 && !hasError;\n    if (!pass) ok = false;\n    rows.push(o.value + ': ' + (pass ? 'ok' : ('FAILED missing=' + JSON.stringify(missing) + ' error=' + hasError)) + ' len=' + txt.length);\n  }\n  await new Promise((r) => setTimeout(r, 3000));\n  const diagrams = Array.from(document.querySelectorAll('.mermaid'));\n  const drawn = diagrams.filter((n) => n.querySelector('svg') && !/Syntax error/i.test(n.textContent)).length;\n  if (diagrams.length !== 2 || drawn !== 2) ok = false;\n  rows.push('diagrams drawn: ' + drawn + ' of ' + diagrams.length);\n  return JSON.stringify({ ok: ok, detail: rows.join(' | ') });\n})())); return JSON.stringify({ ok: a.ok && b.ok, detail: a.detail + ' | explorer panel: ' + b.detail }); })()",
 "dynsyntax|patlang-dynamic-syntax": "(async () => {\n  const sleep = (ms) => new Promise((r) => setTimeout(r, ms));\n  const out = document.getElementById('out-dynsyntax'); out.textContent = '(not run yet)';\n  const btn = Array.from(document.querySelectorAll('button')).find((b) => b.textContent.indexOf('Compile and run') >= 0);\n  const t0 = Date.now(); btn.click(); let txt = '';\n  while (Date.now() - t0 < 120000) { await sleep(300); txt = out.innerText; if (txt !== '(not run yet)' && txt.indexOf('running...') < 0) break; }\n  return JSON.stringify({ ok: txt.indexOf('Hello, World!') >= 0 && txt.indexOf('dynamic syntax demo complete') >= 0, detail: ((Date.now() - t0) / 1000).toFixed(1) + 's: ' + txt.split(String.fromCharCode(10)).join(' | ').slice(0, 160) });\n})()"
}